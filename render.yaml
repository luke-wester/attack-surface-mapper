services:
  - type: web
    name: attack-surface-mapper
    runtime: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: SHODAN_API_KEY
        sync: false
      - key: GITHUB_TOKEN
        sync: false
      - key: APP_PASSWORD
        sync: false
